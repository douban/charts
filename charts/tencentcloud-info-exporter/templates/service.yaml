apiVersion: v1
kind: Service
metadata:
  name: {{ include "tencentcloud-info-exporter.fullname" . }}
  labels:
    {{- include "tencentcloud-info-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tencentcloud-info-exporter.selectorLabels" . | nindent 4 }}
