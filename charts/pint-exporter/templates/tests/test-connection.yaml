apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pint-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "pint-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pint-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
